import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'https://com.diggiassist.app/',
  appName: 'diggiassist',
  webDir: 'out',
};

export default config;
